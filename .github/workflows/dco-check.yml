name: DCO check
on: [pull_request]

jobs:
  dco_check_job:
    name: DCO Check
    runs-on: ubuntu-latest
    steps:
    - name: DCO Check
      uses: tim-actions/dco@master
      with:
        token: ${{ secrets.GITHUB_TOKEN }}